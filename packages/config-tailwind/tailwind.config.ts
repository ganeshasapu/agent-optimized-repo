// Tailwind v4 is CSS-first. This file provides shared theme tokens
// that can be referenced by packages if JS-level config is needed.
export const theme = {
  colors: {
    brand: {
      50: "#eff6ff",
      500: "#3b82f6",
      900: "#1e3a5f",
    },
  },
} as const;
